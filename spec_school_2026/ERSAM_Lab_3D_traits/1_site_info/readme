Reading in GPS locations for foliar samples, hemiphotos, and spectrometer field samples and combinining each group 
with sample unique ID
Reading in the tabular data for each data type
Combining GPS locations with tabular data
figuring out which tiles are needed based GPS locations
possibly figuring out flightlines that line up with GPS locations

Data outputs 
- tables for foliar data, hemiphotos, and spectrometer field samples with GPS locations connected (in UTM coordinates, 
so need column for epsg code too)
- Geopackages (Renae knows what this means) of points for each data type (foliar samples, hemiphotos, and spectrometer field)
- table listing southwest corners of tiles we want (UTMs by 1000 m)
- *** maybe *** table listing flightlines that overlap with foliar samples (note that file naming convention is changing)
  this might be a big pain :( - we have done this before but bespoke site specific scripts.
